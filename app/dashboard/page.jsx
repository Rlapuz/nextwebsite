import { Metadata } from "next";

export const metadata = {
  title: "Raymund | Dashboard",
};

const Dashboard = () => {
  return (
    <>
      <div>Dashboard</div>
    </>
  );
};

export default Dashboard;
